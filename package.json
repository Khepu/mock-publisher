{
  "name": "publisher-tester",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:concurrently:build": "tsc -w",
    "start:concurrently:run": "nodemon build/index.js",
    "start:docker": "cross-env STAGE=DOCKER concurrently yarn:start:concurrently:*",
    "start:dev": "cross-env STAGE=DEV concurrently yarn:start:concurrently:*"
  },
  "dependencies": {
    "amqplib": "^0.8.0",
    "concurrently": "^6.2.1",
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "nodemon": "^2.0.12",
    "rxjs": "^7.3.0",
    "typescript": "^4.3.5",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/amqplib": "^0.8.2",
    "@types/uuid": "^8.3.1"
  }
}
